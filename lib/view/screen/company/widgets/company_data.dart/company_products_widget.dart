import 'package:flutter/material.dart';

class CompanyProductsWidget extends StatelessWidget {
  const CompanyProductsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
