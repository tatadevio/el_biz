import 'package:flutter/material.dart';

class AboutProductWidget extends StatelessWidget {
  const AboutProductWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
